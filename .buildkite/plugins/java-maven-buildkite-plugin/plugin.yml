name: Java package builder
description: Builds a java deployment package using maven
author: https://github.com/madan712
requirements:
    - mvn
configuration:
  properties:
    artifact:
       type: string 
    version:
      type: string
  additionalProperties: false
  required:
    - artifact
    - version